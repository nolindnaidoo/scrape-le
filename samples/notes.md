# Findings

Record reachability, anti-bot vendor, and whether content needs JS before
committing to an approach. A page that renders server-side is an HTTP
request; one that does not is a browser, a queue, and a maintenance burden.
